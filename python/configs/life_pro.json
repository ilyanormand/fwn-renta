{
  "vendor": {
    "name": "Life Pro",
    "currency": "EUR",
    "language": "es"
  },
  "header": {
    "fields": [
      {
        "name": "invoice_number",
        "regex": "ORDER NUM:\\s+(\\w+)",
        "group": 1
      },
      {
        "name": "invoice_date",
        "regex": "DATE:\\s+(\\d{2}/\\d{2}/\\d{4})",
        "group": 1,
        "type": "date"
      }
    ]
  },
  "table": {
    "strategy": "text_regex",
    "start_marker": "REF / EAN\\s+DESCRIPTION",
    "end_marker": "GROSS AMOUNT",
    "row_pattern": "^(?:(\\d{13})\\s+)?(.+?)\\s+([-]?[\\d.,]+)\\s+([-]?[\\d.,]+)(?:\\s+([-]?[\\d.,]+))?\\s+([-]?[\\d.,]+)%\\s+([-]?[\\d.,]+)$",
    "columns": [
      {
        "name": "sku",
        "regex_group": 1,
        "type": "string"
      },
      {
        "name": "description",
        "regex_group": 2,
        "type": "string"
      },
      {
        "name": "quantity",
        "regex_group": 3,
        "type": "number"
      },
      {
        "name": "unit_price",
        "regex_group": 4,
        "type": "number"
      },
      {
        "name": "total",
        "regex_group": 7,
        "type": "number"
      }
    ]
  },
  "footer": {
    "fields": [
      {
        "name": "total",
        "regex": ".*?\\s+((?:\\d{1,3}(?:[\\s.]\\d{3})*),\\d{2})\\s*€\\s*$",
        "group": 1,
        "type": "number"
      }
    ]
  }
}
